
import { increasedIndex } from 'deep-traffic/helpers/increased-index';
import { module, test } from 'qunit';

module('Unit | Helper | increased index');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = increasedIndex([42]);
  assert.ok(result);
});

